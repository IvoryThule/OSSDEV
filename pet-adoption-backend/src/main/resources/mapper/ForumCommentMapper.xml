<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.whu.pet.mapper.ForumCommentMapper">

    <!-- 分页查询评论列表（管理员） -->
    <select id="selectCommentPageWithUser" resultType="com.whu.pet.entity.ForumComment">
        SELECT c.*, u.nickname AS user_name, u.avatar AS user_avatar, p.title AS post_title
        FROM forum_comment c
        LEFT JOIN sys_user u ON c.user_id = u.id
        LEFT JOIN forum_post p ON c.post_id = p.id
        <where>
            <if test="postId != null">
                AND c.post_id = #{postId}
            </if>
            <if test="userId != null">
                AND c.user_id = #{userId}
            </if>
            <if test="keyword != null and keyword != ''">
                AND c.content LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY c.create_time DESC
    </select>

</mapper>
